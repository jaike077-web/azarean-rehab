# 🏥 AZAREAN NETWORK - КОНТЕКСТ ПРОЕКТА
## Полная документация для Claude (обновлено: 18.12.2024)

---

# 📋 СОДЕРЖАНИЕ
1. [Обзор проекта](#обзор-проекта)
2. [Технический стек](#технический-стек)
3. [Архитектура навигации](#архитектура-навигации)
4. [Структура базы данных](#структура-базы-данных)
5. [API Endpoints](#api-endpoints)
6. [Структура файлов](#структура-файлов)
7. [Ключевые компоненты](#ключевые-компоненты)
8. [Текущий статус](#текущий-статус)
9. [Best Practices](#best-practices)
10. [Важные замечания](#важные-замечания)

---

# ОБЗОР ПРОЕКТА

## Цель
Azarean Network — платформа реабилитации для физиотерапевтических студий, специализирующихся на реабилитации плеча и колена.

## Пользователи
1. **Администраторы:** Управление системой, пользователями
2. **Инструкторы:** Создание персонализированных программ упражнений, отслеживание прогресса
3. **Пациенты:** Доступ к программам через уникальные ссылки (БЕЗ регистрации)

## Основной функционал
- Управление пациентами
- Управление диагнозами (новое!)
- Библиотека ~1000 упражнений (видео через Kinescope)
- Создание комплексов упражнений (drag-and-drop)
- Отслеживание прогресса пациентов
- Корзина для удалённых элементов

---

# ТЕХНИЧЕСКИЙ СТЕК

## Frontend
- **React 19**
- **React Router** для навигации
- **Axios** для API запросов
- **@dnd-kit** для drag-and-drop (touch-friendly)
- **lucide-react** для иконок
- **Toast Context** для уведомлений

## Backend
- **Node.js** с Express
- **JWT** для авторизации
- **bcrypt** для хеширования паролей
- **helmet.js** для безопасности
- **express-rate-limit** для защиты от брутфорса
- **CORS** настроен

## База данных
- **PostgreSQL**
- Путь к функциям: `database/db.js`
- Функция запросов: `query()` (НЕ pool.query!)

## Видео
- **Kinescope API** для хостинга упражнений
- Thumbnails через API

## Разработка
- **IDE:** Cursor (Windows)
- **Backend порт:** 5000
- **Frontend порт:** 3000

---

# АРХИТЕКТУРА НАВИГАЦИИ

## Dashboard.js — главный контейнер
```
Dashboard (с боковым меню) → activeTab переключает:
├── Patients.js (внутри Dashboard)
├── Diagnoses.js (внутри Dashboard) ← НОВОЕ!
├── CreateComplex.js (внутри Dashboard)
├── MyComplexes.js (внутри Dashboard)
└── Trash.js (внутри Dashboard)
```

## Отдельные роуты (за пределами Dashboard)
```
/exercises → Exercises.js (библиотека)
/exercises/:id → ExerciseDetail.js
/progress/:id → ViewProgress.js
/patient/:token → PatientView.js (публичный, БЕЗ авторизации)
/edit-complex/:id → EditComplex.js
```

## Важно!
- **BackButton** и **Breadcrumbs** нужны ТОЛЬКО на отдельных роутах
- **НЕ используй** их внутри Dashboard-вкладок

---

# СТРУКТУРА БАЗЫ ДАННЫХ

## Таблица: users
```sql
- id (PRIMARY KEY)
- email (UNIQUE, NOT NULL)
- password_hash (NOT NULL)
- full_name
- role (admin/instructor)
- created_at
```

## Таблица: patients
```sql
- id (PRIMARY KEY)
- full_name (NOT NULL)
- email
- phone
- birth_date
- diagnosis (TEXT)
- notes
- created_by (FK → users.id)
- is_active (BOOLEAN, default true)
- created_at
- updated_at
```

## Таблица: diagnoses ← НОВАЯ!
```sql
- id (PRIMARY KEY)
- name (TEXT, NOT NULL)
- category (TEXT)
- description (TEXT)
- recommendations (TEXT)
- warnings (TEXT)
- is_active (BOOLEAN)
- created_at
- updated_at
- deleted_at (для soft delete)
```

## Таблица: exercises
```sql
- id (PRIMARY KEY)
- title (NOT NULL)
- video_url (Kinescope)
- thumbnail_url
- body_region (shoulder/knee/lower_back/neck)
- difficulty_level (beginner/intermediate/advanced)
- equipment (JSONB array)
- muscle_groups (JSONB array)
- tags (JSONB array)
- description
- created_at
```

## Таблица: complexes
```sql
- id (PRIMARY KEY)
- patient_id (FK → patients.id)
- access_token (UNIQUE, для пациентов)
- diagnosis_id (FK → diagnoses.id) ← СВЯЗЬ!
- notes
- recommendations
- warnings
- is_active (BOOLEAN)
- created_at
- updated_at
```

## Таблица: complex_exercises
```sql
- id (PRIMARY KEY)
- complex_id (FK → complexes.id)
- exercise_id (FK → exercises.id)
- order_number (INT)
- sets (INT)
- reps (INT)
- duration (INT, seconds)
- rest_time (INT, seconds)
```

## Таблица: progress_logs
```sql
- id (PRIMARY KEY)
- complex_id (FK → complexes.id)
- exercise_id (FK → exercises.id)
- completed (BOOLEAN)
- pain_level (INT 0-10)
- notes
- completed_at
```

---

# API ENDPOINTS

## Аутентификация
```
POST   /api/auth/login
POST   /api/auth/register
GET    /api/auth/me
```

## Пациенты
```
GET    /api/patients (свои пациенты)
GET    /api/patients/:id
POST   /api/patients
PUT    /api/patients/:id
DELETE /api/patients/:id (soft delete)
GET    /api/patients/trash
PATCH  /api/patients/:id/restore
DELETE /api/patients/:id/permanent
```

## Диагнозы ← НОВОЕ!
```
GET    /api/diagnoses (все диагнозы)
GET    /api/diagnoses/:id
POST   /api/diagnoses
PUT    /api/diagnoses/:id
DELETE /api/diagnoses/:id (soft delete)
POST   /api/diagnoses/:id/restore
```

## Комплексы
```
POST   /api/complexes
GET    /api/complexes/:id
GET    /api/complexes/token/:token (публичный)
GET    /api/complexes/patient/:patientId
GET    /api/complexes
PUT    /api/complexes/:id
DELETE /api/complexes/:id (soft delete)
GET    /api/complexes/trash/list
PATCH  /api/complexes/:id/restore
DELETE /api/complexes/:id/permanent
```

## Упражнения
```
GET    /api/exercises (с фильтрами)
GET    /api/exercises/:id
POST   /api/exercises
PUT    /api/exercises/:id
DELETE /api/exercises/:id
GET    /api/exercises/muscle-groups/all
GET    /api/exercises/tags/all
POST   /api/exercises/tags
GET    /api/exercises/:id/presets
```

## Прогресс
```
POST   /api/progress (БЕЗ auth - для пациента)
GET    /api/progress/complex/:id (БЕЗ auth)
GET    /api/progress/complex/:id (С auth - для инструктора)
```

## Dashboard
```
GET    /api/dashboard/stats
```

---

# СТРУКТУРА ФАЙЛОВ

## Backend
```
backend/
├── database/
│   └── db.js ← Функция query() здесь!
├── middleware/
│   └── auth.js ← authenticateToken
├── routes/
│   ├── auth.js
│   ├── patients.js
│   ├── diagnoses.js ← НОВЫЙ!
│   ├── complexes.js
│   ├── exercises.js
│   ├── progress.js
│   └── dashboard.js
├── .env
└── server.js
```

## Frontend
```
frontend/
├── public/
│   └── AN_logo.jpg
├── src/
│   ├── context/
│   │   ├── AuthContext.js
│   │   └── ToastContext.js ← Уведомления
│   ├── pages/
│   │   ├── Dashboard.js ← Главный контейнер
│   │   ├── Patients.js
│   │   ├── Patients.css
│   │   ├── Diagnoses.js ← НОВЫЙ!
│   │   ├── Diagnoses.css ← НОВЫЙ!
│   │   ├── CreateComplex.js
│   │   ├── MyComplexes.js
│   │   ├── Trash.js
│   │   ├── Exercises.js (отдельный роут)
│   │   ├── ExerciseDetail.js
│   │   ├── ViewProgress.js
│   │   ├── PatientView.js
│   │   └── EditComplex.js
│   ├── components/
│   │   ├── BackButton.js
│   │   ├── Breadcrumbs.js
│   │   └── Skeleton.js ← Loading placeholders
│   ├── services/
│   │   └── api.js ← Все API методы
│   ├── App.js
│   └── index.js
└── package.json
```

---

# КЛЮЧЕВЫЕ КОМПОНЕНТЫ

## ToastContext
```javascript
import { useToast } from '../context/ToastContext';

const toast = useToast();
toast.success('Успешно!');
toast.error('Ошибка!');
```

## API Service Pattern
```javascript
// В api.js
export const diagnoses = {
  getAll: () => api.get('/diagnoses'),
  getById: (id) => api.get(`/diagnoses/${id}`),
  create: (data) => api.post('/diagnoses', data),
  update: (id, data) => api.put(`/diagnoses/${id}`, data),
  delete: (id) => api.delete(`/diagnoses/${id}`),
};

// В компоненте
import { diagnoses } from '../services/api';

const response = await diagnoses.getAll();
const data = response.data?.diagnoses || response.diagnoses || [];
```

## Backend Route Pattern
```javascript
const express = require('express');
const router = express.Router();
const { query } = require('../database/db'); // ← ВАЖНО!
const { authenticateToken } = require('../middleware/auth');

router.get('/', authenticateToken, async (req, res) => {
  try {
    const result = await query('SELECT * FROM table');
    res.json({ success: true, data: result.rows });
  } catch (error) {
    console.error('Ошибка:', error);
    res.status(500).json({ 
      success: false, 
      message: 'Ошибка' 
    });
  }
});

module.exports = router;
```

## Dashboard Tab Pattern
```javascript
// В Dashboard.js
const [activeTab, setActiveTab] = useState('patients');

const renderContent = () => {
  switch (activeTab) {
    case 'patients': return <Patients />;
    case 'diagnoses': return <Diagnoses />; // ← НОВОЕ
    // ...
  }
};

// Кнопка меню
<button 
  className={`nav-item ${activeTab === 'diagnoses' ? 'active' : ''}`}
  onClick={() => handleNavClick('diagnoses')}
>
  <FileText className="nav-icon" size={18} />
  <span>Диагнозы</span>
</button>
```

---

# ТЕКУЩИЙ СТАТУС

## ✅ Завершено (95% готово)
- [x] Аутентификация (JWT)
- [x] Управление пациентами (CRUD + корзина)
- [x] Библиотека упражнений с фильтрами
- [x] Создание комплексов (drag-and-drop)
- [x] Отслеживание прогресса пациентов
- [x] Корзина для комплексов
- [x] Toast уведомления
- [x] Touch-friendly DnD (@dnd-kit)
- [x] Безопасность (helmet, rate-limit, CORS)
- [x] Адаптивный дизайн (fluid CSS, clamp())
- [x] **Модуль "Диагнозы"** ← ДОБАВЛЕНО СЕГОДНЯ!

## 🚧 В разработке
- [ ] Loading skeletons (частично)
- [ ] Шаблоны комплексов
- [ ] WhatsApp интеграция

## 🎯 Текущий скор: 85/100
- Desktop функционал: 95/100
- Mobile адаптация: 70/100
- Безопасность: 90/100
- Testing: 0/100 (не реализовано)

---

# BEST PRACTICES

## 1. Database Queries
```javascript
// ✅ ПРАВИЛЬНО
const { query } = require('../database/db');
const result = await query('SELECT * FROM table WHERE id = $1', [id]);

// ❌ НЕПРАВИЛЬНО
const pool = require('../config/database'); // НЕТ такой папки!
const result = await pool.query(...);
```

## 2. API Response Handling
```javascript
// Всегда используй fallback
const response = await api.get('/endpoint');
const data = response.data?.items || response.items || [];
```

## 3. Soft Delete Pattern
```javascript
// Не удаляй из БД, используй флаг
UPDATE table SET deleted_at = NOW() WHERE id = $1;
// Восстановление
UPDATE table SET deleted_at = NULL WHERE id = $1;
```

## 4. CSS Responsive Design
```css
/* ✅ ПРАВИЛЬНО - Fluid design */
font-size: clamp(14px, 2vw, 16px);
padding: clamp(12px, 3vw, 20px);
grid-template-columns: repeat(auto-fill, minmax(min(320px, 100%), 1fr));

/* ❌ НЕПРАВИЛЬНО - Fixed breakpoints */
@media (max-width: 768px) { font-size: 14px; }
@media (min-width: 769px) { font-size: 16px; }
```

## 5. Component File Organization
```
ВНУТРИ Dashboard (вкладки):
- Patients.js / Patients.css
- Diagnoses.js / Diagnoses.css
- CreateComplex.js / CreateComplex.css

ВНЕ Dashboard (отдельные роуты):
- Exercises.js / Exercises.css
- ExerciseDetail.js / ExerciseDetail.css
```

## 6. Button Styling Pattern
```css
/* Стандартные action buttons (как в Patients) */
.btn-card-action {
  padding: clamp(10px, 2vw, 12px) clamp(14px, 2vw, 16px);
  border: 1px solid #e2e8f0;
  background: white;
  border-radius: 10px;
  min-height: 44px; /* Touch-friendly */
}

.btn-edit {
  color: #3182ce;
  border-color: #bee3f8;
  background: #ebf8ff; /* ← Обязательно */
}

.btn-delete {
  color: #c53030;
  border-color: #feb2b2;
  background: #fff5f5; /* ← Обязательно */
}
```

---

# ВАЖНЫЕ ЗАМЕЧАНИЯ

## 1. Импорты в Backend Routes
```javascript
// ✅ ПРАВИЛЬНО
const { query } = require('../database/db');
const { authenticateToken } = require('../middleware/auth');

// ❌ НЕПРАВИЛЬНО
const pool = require('../config/database');
const { auth } = require('../middleware/auth');
```

## 2. Добавление Роута в server.js
```javascript
// В server.js после создания app
const app = express();

// ... middleware ...

// Роуты ПОСЛЕ инициализации app
app.use('/api/diagnoses', require('./routes/diagnoses'));
```

## 3. Dashboard Navigation
```javascript
// Импорты
import Diagnoses from './Diagnoses';
import { FileText } from 'lucide-react';

// В renderContent()
case 'diagnoses': return <Diagnoses />;

// Кнопка
<button className={`nav-item ${activeTab === 'diagnoses' ? 'active' : ''}`}>
  <FileText className="nav-icon" size={18} />
  <span>Диагнозы</span>
</button>
```

## 4. API Service
```javascript
// В api.js ПОСЛЕ export default api
export const diagnoses = {
  getAll: () => api.get('/diagnoses'),
  getById: (id) => api.get(`/diagnoses/${id}`),
  create: (data) => api.post('/diagnoses', data),
  update: (id, data) => api.put(`/diagnoses/${id}`, data),
  delete: (id) => api.delete(`/diagnoses/${id}`),
};
```

## 5. CSS Class Names
- Dashboard tabs: `.nav-item`
- Card actions: `.btn-card-action`
- Modals: `.modal-overlay`, `.modal-content`
- Forms: `.form-group`

## 6. Responsive Approach
Используй **fluid design** вместо fixed breakpoints:
- `clamp()` для размеров
- `min()` для max-width
- `repeat(auto-fill, minmax())` для grid
- Минимум медиа-запросов

## 7. Touch-Friendly
- `min-height: 44px` для всех кнопок
- `@dnd-kit` с TouchSensor
- Достаточные gap между элементами

## 8. Error Handling
```javascript
try {
  const response = await api.call();
  const data = response.data?.field || response.field || [];
  // ...
} catch (error) {
  console.error('Ошибка:', error);
  toast.error(error.response?.data?.message || 'Общая ошибка');
}
```

---

# МОДУЛЬ "ДИАГНОЗЫ" (СОЗДАН СЕГОДНЯ)

## Файлы
- `backend/routes/diagnoses.js` - CRUD API
- `frontend/src/pages/Diagnoses.js` - Компонент
- `frontend/src/pages/Diagnoses.css` - Стили

## Функционал
1. ✅ Список диагнозов (сетка карточек)
2. ✅ Поиск по названию/описанию
3. ✅ Добавление диагноза (модалка с формой)
4. ✅ Редактирование диагноза
5. ✅ Удаление диагноза (soft delete)
6. ✅ Просмотр деталей (клик по карточке)
7. ✅ Адаптивный дизайн

## Поля диагноза
- **name** (обязательно) - Название
- **description** - Описание патологии
- **recommendations** - Рекомендации для пациента
- **warnings** - Предостережения

## Интеграция с CreateComplex
В CreateComplex шаг 2 "Диагноз":
- Выбор диагноза из dropdown
- Автоподстановка recommendations и warnings
- Можно редактировать под конкретного пациента

---

# ЧТО СДЕЛАТЬ ПРИ ПРОБЛЕМАХ

## Backend не стартует
1. Проверь `.env` файл (DATABASE_URL, JWT_SECRET)
2. Проверь подключение к PostgreSQL
3. Проверь импорты: `const { query } = require('../database/db');`

## Frontend не компилируется
1. Проверь импорты компонентов
2. Проверь `api.js` - нет дубликатов export
3. Очисти кэш: `npm cache clean --force`
4. Переустанови: `rm -rf node_modules && npm install`

## Стили не применяются
1. Жёсткая перезагрузка: `Ctrl + Shift + R`
2. Проверь правильность пути к CSS
3. Проверь className в JSX

## API 404
1. Проверь что роут добавлен в `server.js`
2. Проверь путь в `api.js`
3. Проверь что backend запущен

---

# ДАЛЬНЕЙШИЕ ПЛАНЫ (v2.0)

## Приоритет 1
- [ ] Loading skeletons для всех страниц
- [ ] Automated testing (Jest + React Testing Library)
- [ ] Шаблоны комплексов

## Приоритет 2
- [ ] WhatsApp интеграция (отправка ссылок)
- [ ] Email уведомления
- [ ] Экспорт прогресса в PDF

## Приоритет 3
- [ ] Real-time прогресс (WebSocket)
- [ ] Расширенная аналитика
- [ ] Мобильное приложение (React Native)

---

# КОНТАКТЫ / СРЕДА

- **Разработчик:** Vadim
- **IDE:** Cursor (Windows)
- **Язык интерфейса:** Русский
- **Дата последнего обновления:** 18.12.2024

---

# БЫСТРЫЕ КОМАНДЫ

```bash
# Backend
cd backend
npm run dev

# Frontend
cd frontend
npm start

# PostgreSQL
psql -U postgres -d azarean_db

# Git
git add .
git commit -m "feat: добавлен модуль диагнозы"
git push
```

---

**Этот файл содержит ВСЁ что нужно знать о проекте для продолжения разработки! 🚀**
