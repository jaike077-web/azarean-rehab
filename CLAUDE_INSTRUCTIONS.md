# Azarean Network — Инструкция для Claude

## Общая информация

Проект: **Azarean Network** — платформа реабилитации для физиотерапевтических центров (плечо, колено).
Разработчик: Вадим
Стек: React 19, Node.js/Express, PostgreSQL, Cursor IDE (Windows)
Видео: Kinescope API

## Роли пользователей

- **Администраторы**: управление системой, пользователями, библиотекой упражнений
- **Инструкторы**: создание комплексов через drag-and-drop, отслеживание прогресса
- **Пациенты**: доступ к комплексам по уникальной ссылке (без регистрации)

## Архитектура навигации

**Dashboard.js** — главный контейнер с боковым меню (бургер на мобильных)
- Использует `activeTab` для переключения между вкладками
- Вкладки (рендерятся ВНУТРИ Dashboard): 
  - `Patients.js`
  - `CreateComplex.js` 
  - `MyComplexes.js`
  - `Trash.js`

**Отдельные роуты** (самостоятельные страницы):
- `/exercises` → `Exercises.js`
- `/exercises/:id` → `ExerciseDetail.js`
- `/progress/:id` → `ViewProgress.js`
- `/patient/:token` → `PatientView.js` (публичный)
- `/edit-complex/:id` → `EditComplex.js`

**Важно:** BackButton и Breadcrumbs нужны только на отдельных роутах, НЕ внутри Dashboard-вкладок.

## Структура БД

```
users (id, email, password_hash, full_name, role)
patients (id, full_name, email, phone, birth_date, notes, created_by)
exercises (id, title, video_url, body_region, difficulty_level, equipment JSONB, thumbnail_url)
complexes (id, patient_id, access_token, diagnosis_id, recommendations, warnings)
complex_exercises (complex_id, exercise_id, order_number, sets, reps, notes)
progress_logs (complex_id, exercise_id, pain_level, completed, session_number)
diagnoses (id, name, recommendations, warnings)
```

## Ключевые компоненты

- `ToastContext` — уведомления: `toast.success()`, `toast.error()`, `toast.warning()`
- `Skeleton.js` — loading-плейсхолдеры (создан, не везде внедрён)
- `@dnd-kit` — drag-and-drop с TouchSensor для мобильных
- `lucide-react` — библиотека иконок

## Текущий статус (90/100)

### ✅ Выполнено:
- Безопасность (helmet, rate-limit, JWT, CORS)
- Toast уведомления (заменили все alert())
- Touch-friendly DnD
- Dashboard мобильное бургер-меню
- CreateComplex редизайн для мобильных (файлы готовы, нужна проверка)

### 🔄 В процессе:
- Мобильная адаптация CreateComplex — проверить после замены файлов
- Loading skeletons — компоненты созданы, нужно внедрить

### ⏳ Ожидает:
- Мобильная адаптация: Patients, MyComplexes, Trash, EditComplex
- Мобильная адаптация: Exercises (частично готова)
- Шаблоны комплексов
- WhatsApp интеграция

## Известные проблемы

1. **nav-overlay перекрывает клики** — исправлено через `pointer-events: none`
2. **Тень от скрытого меню** — исправлено, `box-shadow` только на `.dashboard-nav.open`
3. **Ошибка 500 при больших числах** — добавлена валидация в CreateComplex (max 9999)

## Принципы работы

1. **База данных**: проверять схему, использовать `response.data || response`
2. **UX**: интуитивная навигация, различать обязательные/опциональные поля
3. **Безопасность**: секреты в .env, JWT с ротацией
4. **Код**: разделение frontend/backend/db, единые соглашения, обработка ошибок

## Стиль ответов

- Краткие технические ответы без лишних эмодзи и таблиц
- Код с комментариями на критических участках
- Сначала анализ, потом решение
- Файлы сразу готовые к использованию

## Пути к файлам

```
frontend/src/
├── pages/ (Dashboard, Patients, CreateComplex, MyComplexes, Trash, Exercises, PatientView, ViewProgress, EditComplex)
├── components/ (BackButton, Breadcrumbs, Toast, Skeleton)
├── context/ (AuthContext, ToastContext)
├── services/api.js
└── App.js

backend/
├── routes/ (auth, patients, exercises, complexes, progress, diagnoses)
├── middleware/ (auth.js)
└── server.js
```

## Как начать новый чат

1. Скинь эту инструкцию
2. Опиши текущую задачу
3. Приложи только релевантные файлы (CSS/JS той страницы, которую правим)
