# Скрипты для начала диалога с Claude Code — Волна 0

**Дата:** 2026-05-08
**Использование:** Vadim копирует подходящий блок в начало нового чата с Claude Code. Один блок = одна сессия = один коммит.

**Принцип:** новая сессия Claude Code на каждый коммит (правило `feedback_one_change_per_session.md`). Не миксовать.

---

## Универсальный заголовок (одинаковый для всех 6 сессий)

Этот блок копируется в **начало каждого** диалога с Claude Code, с подстановкой номера коммита.

Удобнее всего — сохранить шаблон в локальный snippet manager (Espanso, Alfred, etc.) и каждый раз менять только две строки: номер коммита и название TZ-файла.

---

## 🔵 Сессия #01 — fix streak no reset

```
Привет, Claude Code.

Контекст: проект Azarean Rehab (https://my.azarean.ru), платформа реабилитации физиотерапевтической студии. Я Vadim, со-владелец и физтер. Архитектура работы: внешний архитектор пишет ТЗ → ты исполняешь по одному коммиту за сессию → я ревьюю → push в main по моему явному «ок».

Мы начинаем Волну 0 — серию из 6 атомарных коммитов (срочные фиксы регрессий и quick wins) перед Волной 1 (multi-protocol). Это первая сессия — коммит #01.

## С чего начать (ровно в этом порядке)

1. Прочитай `TZ_WAVE_0_INDEX.md` в корне проекта — карта всей волны, принципы исполнения, DoD.
2. Прочитай `PATIENT_UX_ROADMAP_2026-05-08_v2.md` секции про стрик (#7) и Star Tracker — это контекст: какое решение принято и почему.
3. Прочитай `TZ_WAVE_0_01_streak_no_reset.md` — конкретный план работы для этого коммита.
4. Создай файл `wave_0_progress.md` в корне проекта (формат описан в INDEX, секция `wave_0_progress.md — журнал прогресса`).
5. Создай feature-branch `wave-0/01-streak-no-reset` от свежего `origin/main`. Работаем НЕ на main.
6. Исполни ТЗ пошагово, ничего не пропуская.

## Критичные правила (повторяю из INDEX, не пропускать)

⛔ **STOP в конце.** После `git commit` ты ЗАКАНЧИВАЕШЬ сессию: обновляешь `wave_0_progress.md` (статус 🟡 готов к smoke), отчитываешься мне в чате о выполненной работе, и ЖДЁШЬ моего явного «ок» на push в main. Не делаешь push без подтверждения.

⛔ **Один коммит за сессию.** НЕ открывай TZ_WAVE_0_02 в этой же сессии, даже если коммит #01 закрыт. Соблазн «давай сразу следующий раз остался контекст» — это сигнал остановиться. Следующий коммит — НОВАЯ сессия со своим промптом.

⛔ **НЕ выдумывать.** Если в ТЗ что-то непонятно или расходится с реальностью кода:
- Файл не найден по указанному пути → стоп, спроси
- Линия отличается от того что в ТЗ (~строка 564 на самом деле строка 612) → ок, найди по контексту, но скажи мне в отчёте
- Тест падает не очевидно → стоп, обсудим
- Поведение отличается от ожидаемого → стоп
- Поле БД отсутствует или имеет другой тип → стоп
- Вообще любой WTF-момент → стоп

⛔ **НЕ миксовать.** Этот коммит — только про стрик. НЕ трогай ничего другого, даже если видишь рядом баг (HomeScreen литерал «ПКС», DiaryScreen clipboard и т.д.). Это всё в отдельных коммитах #02-06. Не делай «заодно».

⛔ **НЕ дёргать LOCKED-зону.** В этом коммите она не нужна (стрик не лезет в ExerciseRunner). Если вдруг ТЗ просит — это ошибка ТЗ, спроси меня.

⛔ **Smoke в реальном браузере обязателен.** После всех изменений — открой `http://localhost:3001`, войди как пациент `avi707@mail.ru` / `Test1234`, пройди smoke-сценарии 1-4 из ТЗ. НЕ ОГРАНИЧИВАЙСЯ jest-тестами, особенно для UI-частей. Прецедент c8834b5 — табу.

## Технический стек (повторяю на всякий случай)

- Backend: Node.js CommonJS (НЕ ESM), Express 5, raw SQL через `query()` wrapper из `database/db.js` (НЕ ORM, НЕ pool напрямую), параметризованные запросы `$1, $2`
- Frontend: React 19 + CRA + JavaScript (НЕ TypeScript), глобальные CSS с prefix `pd-` для PatientDashboard, lucide-react иконки (НЕ emoji)
- API response format: `{ data, message?, total? }` — НЕ добавлять `success: true`
- Telegram: node-telegram-bot-api 0.67 (НЕ Telegraf, не как в JARVIS)
- Комментарии в коде — на русском
- БД-арифметика только через код, НЕ в уме

## Как сообщать о прогрессе

После каждого крупного шага коммита (миграция применилась / backend изменения завершены / frontend изменения завершены / тесты прошли):
- Кратко в чат: «Шаг X выполнен — [что сделал, какие файлы]»

В конце сессии (после `git commit`):
- Показать `git diff main...HEAD` (или `git show HEAD`)
- Показать обновлённый `wave_0_progress.md`
- Список файлов: создано / изменено
- Результаты тестов (backend + frontend)
- Чеклист DoD из ТЗ — отметить что выполнено
- Запрос: «Готов smoke в реальном браузере по сценариям 1-4. После твоего «ок» — push в main»

## Контактные детали

- Test patient (для smoke в dev): id=14, `avi707@mail.ru` / `Test1234`
- Test instructor: `vadim@azarean.com` / `Test1234`
- Prod URL: https://my.azarean.ru (НЕ трогать без push в main)
- Repo: https://github.com/jaike077-web/azarean-rehab.git
- VDS shared с JARVIS Director — НЕ ТРОГАТЬ JARVIS код (порт 3000, отдельный репо)

## Поехали

Прочитай документы по порядку (INDEX → roadmap_v2 секции стрик/Star Tracker → TZ_WAVE_0_01), создай wave_0_progress.md, создай feature-branch, и приступай. Спрашивай если что-то неоднозначно.
```

---

## 🔵 Сессия #02 — feat home dynamic label

```
Привет, Claude Code.

Контекст: Azarean Rehab, Волна 0. Сессия #02 (предыдущая #01 «fix streak no reset» закрыта в main, проверь last commit на main и `wave_0_progress.md`).

## С чего начать

1. Убедись что ты на main и подтянул последний commit Волны 0 (`git checkout main && git pull origin main`)
2. Проверь `wave_0_progress.md` — статус коммита 01 должен быть 🟢 в main. Если нет — стоп, я не закрыл предыдущий.
3. Прочитай `TZ_WAVE_0_INDEX.md` (если ещё не читал в этой машине)
4. Прочитай `TZ_WAVE_0_02_home_dynamic_label.md` — это план работы
5. Прочитай `PATIENT_UX_ROADMAP_2026-05-08_v2.md` секция #1 (не-ПКС пациент) для контекста почему именно так
6. Создай feature-branch `wave-0/02-home-dynamic-label` от свежего main

## Что НЕ делать в этом коммите

- НЕ вводить поле `rehab_programs.program_type` (это Волна 1, не сейчас)
- НЕ создавать таблицу `program_types` (тоже Волна 1)
- НЕ трогать `RoadmapScreen.js` или `services/telegramBot.js` — там тоже есть хардкод `'acl'` но это в Волне 1
- НЕ трогать `HomeScreen.js` за пределами литерала «ПКС» — секция «разблок повторного захода» это коммит #04

В этом коммите — минимальный фикс: вместо литерала «ПКС Фаза N» показывать `program_label` из dashboard, который вычисляется backend'ом из существующего поля `rehab_programs.diagnosis` через простой mapping. Это временное решение до Волны 1, помеченное TODO в коде.

## Критичные правила (повтор из INDEX)

⛔ STOP после `git commit`, ждать «ок» на push
⛔ Один коммит за сессию
⛔ НЕ выдумывать (если что-то расходится — стоп, спроси)
⛔ Smoke в реальном браузере обязателен (Chrome desktop минимум)
⛔ НЕ открывать TZ_WAVE_0_03 после закрытия #02

## Как сообщать о прогрессе

То же что в #01: шаги в чат, в конце diff + DoD-чеклист + запрос smoke.

## Поехали
```

---

## 🔵 Сессия #03 — feat diary report via messages

```
Привет, Claude Code.

Контекст: Azarean Rehab, Волна 0. Сессия #03 (предыдущие #01 streak и #02 home label закрыты).

## С чего начать

1. `git checkout main && git pull origin main`
2. Проверь `wave_0_progress.md` — статусы #01 и #02 = 🟢 в main
3. Прочитай `TZ_WAVE_0_INDEX.md` если ещё не читал
4. Прочитай `TZ_WAVE_0_03_diary_report_via_messages.md`
5. Прочитай `PATIENT_UX_ROADMAP_2026-05-08_v2.md` секция #6 (отчёт по занятию)
6. Создай feature-branch `wave-0/03-diary-report-via-messages`

## Что НЕ делать в этом коммите

- НЕ создавать `session_summary` VIEW (это Волна 3, отдельная история)
- НЕ добавлять поле `complexes.kind` (тоже Волна 3)
- НЕ строить «Историю тренировок» UI (Волна 3)
- НЕ трогать `progress_logs` структуру

В этом коммите — переключить «Отправить отчёт» в DiaryScreen с `clipboard + MessengerCTA` на `POST в /api/rehab/my/messages` с `linked_diary_id` и `message_kind = 'diary_report'`. MessengerCTA остаётся как опциональная кнопка «Также продублировать в Telegram».

Backend: расширить existing endpoint, возможно добавить колонку `message_kind` в `messages` (миграция).

## Критичные правила

⛔ Те же. STOP после commit, smoke, один коммит за сессию.

## Поехали
```

---

## 🔵 Сессия #04 — feat home replay

```
Привет, Claude Code.

Контекст: Azarean Rehab, Волна 0. Сессия #04 (предыдущие #01-03 закрыты).

## С чего начать

1. `git checkout main && git pull origin main`
2. Проверь `wave_0_progress.md` — статусы #01, #02, #03 = 🟢 в main
3. Прочитай `TZ_WAVE_0_INDEX.md` если ещё не читал
4. Прочитай `TZ_WAVE_0_04_home_replay.md`
5. Прочитай `PATIENT_UX_ROADMAP_2026-05-08_v2.md` секция #8 (3-5 раз в день)
6. Создай feature-branch `wave-0/04-home-replay`

## Что важно

Это **самый маленький коммит волны** (frontend only, без backend, без миграций). Объём 2-3 часа.

После завершения тренировки — на HomeScreen появляется secondary CTA «Начать ещё раз ↻» рядом с подтверждением «Тренировка завершена».

## Что НЕ делать

- НЕ вводить counter «1/5» (это Волна 3 с frequency_per_day)
- НЕ менять `dashboardData.todayDone` boolean → counter (Волна 3)
- НЕ трогать backend
- НЕ создавать миграции

Минимальный фикс: разблокировать кнопку, не вводить логики frequency.

## Критичные правила

⛔ Те же. STOP после commit, smoke в браузере (визуально проверить что secondary CTA появилась, тапается, открывает комплекс заново), один коммит за сессию.

## Поехали
```

---

## 🔵 Сессия #05 — feat runner accordion extended (LOCKED разрешено)

```
Привет, Claude Code.

Контекст: Azarean Rehab, Волна 0. Сессия #05 (предыдущие #01-04 закрыты). Этот коммит — самый рискованный в волне, читай внимательно.

## С чего начать

1. `git checkout main && git pull origin main`
2. Проверь `wave_0_progress.md` — статусы #01-04 = 🟢 в main
3. Прочитай `TZ_WAVE_0_INDEX.md` если ещё не читал
4. Прочитай `TZ_WAVE_0_05_runner_accordion_extended.md` — план работы
5. Прочитай `PATIENT_UX_ROADMAP_2026-05-08_v2.md` секция #4 (описание упражнения)
6. **Прочитай `CLAUDE.md` секцию «LOCKED — Не изменять без явного запроса»** — там описание ExerciseRunner v3 LOCKED-режима
7. Создай feature-branch `wave-0/05-runner-accordion`

## ⚠️ ВНИМАНИЕ — LOCKED разрешение

ExerciseRunner v3 помечен LOCKED в `CLAUDE.md`. Vadim **разрешил** трогать этот файл для расширения accordion. Разрешение действует ТОЛЬКО для этого коммита.

**Что разрешено трогать в `ExerciseRunner.js`:**
- Текст внутри accordion-секции (добавить новые секции, расширить существующие)
- Импорт новых иконок lucide-react
- Локальные стили secций безопасности (через `<style>` JSX или CSS-классы)

**Что ВСЁ ЕЩЁ ЗАПРЕЩЕНО:**
- Timer / RestTimer / RPE zones / pain gradient slider — НЕ ТРОГАТЬ
- Slide-in анимации `pdIn` / `pdBk` — НЕ ТРОГАТЬ
- Layout `.pg` wrapper, media queries (480/769/1801) — НЕ ТРОГАТЬ
- CSS-классы `.crd/.sec/.dot/.btn/.tmr/.fb-toggle/.rpe-b/.pv/.plbl/.cmt` — НЕ ПЕРЕИМЕНОВЫВАТЬ
- iOS-палитра `--az-*` — НЕ МЕНЯТЬ
- Flow «HomeScreen → Начать → ExerciseRunner» — НЕ МЕНЯТЬ

Если в процессе тебе кажется что нужно тронуть что-то из запрещённого списка — СТОП, спроси.

## Smoke ОСОБЕННО важен

Этот коммит — UI-heavy. CSS Modules dash-case incident (`c8834b5`) случился из-за того что моки тестов не ловили undefined класс. **Smoke в реальном браузере критически обязателен:**

1. Войти как пациент `avi707@mail.ru` / `Test1234`
2. HomeScreen → «Начать тренировку» → пройти первые 2-3 экрана упражнений
3. Раскрыть accordion («Описание и инструкции») — проверить:
   - Все 4 секции отображаются (description, how_to+cues, tips, safety)
   - Иконки lucide-react видны (НЕ emoji)
   - Стили в светлой теме читаемы
   - Стили в тёмной теме читаемы (toggle на ProfileScreen)
   - Timer/RPE/pain slider работают как раньше — НЕ СЛОМАНЫ
   - Анимации перехода между упражнениями работают
4. Проверить что safe_with_inflammation badge на ExerciseCard показывается у нужных упражнений (в библиотеке инструктора)
5. Проверить ExerciseModal у инструктора — hint'ы рядом с полями видны

## Что НЕ делать

- НЕ удалять колонки exercises (cues, tips, absolute_contraindications, red_flags) — это другой коммит, миграция нужна осторожная
- НЕ менять ExerciseRunner core (timer/RPE/pain) даже «случайно»
- НЕ переименовывать CSS-классы LOCKED-зоны
- НЕ создавать новый компонент `ExerciseInfoAccordion` — мы делаем inline в ExerciseRunner (это решение Vadim'а)

## Критичные правила

⛔ STOP после commit, smoke ОБЯЗАТЕЛЕН, один коммит за сессию.
⛔ Если visual regression в LOCKED-зоне — стоп немедленно, откати.
⛔ Если тест на ExerciseRunner ломается — стоп, разберёмся вместе.

## Поехали
```

---

## 🔵 Сессия #06 — feat roadmap stuck banner

```
Привет, Claude Code.

Контекст: Azarean Rehab, Волна 0. Сессия #06 — последний коммит волны.

## С чего начать

1. `git checkout main && git pull origin main`
2. Проверь `wave_0_progress.md` — статусы #01-05 = 🟢 в main
3. Прочитай `TZ_WAVE_0_INDEX.md` если ещё не читал
4. Прочитай `TZ_WAVE_0_06_roadmap_stuck_banner.md`
5. Прочитай `PATIENT_UX_ROADMAP_2026-05-08_v2.md` секция #2 (застрявший пациент)
6. Создай feature-branch `wave-0/06-roadmap-stuck-banner`

## Что важно

Простой info-баннер на RoadmapScreen для пациентов которые застряли на фазе дольше чем `duration_weeks × 1.5`. CTA «Связаться с куратором» с pre-filled сообщением.

Новый endpoint: `GET /api/rehab/my/stuck-status`. Без cron, без push'а инструктору, без чек-листа критериев перехода.

## Что НЕ делать

- НЕ создавать чек-лист критериев с автоматической самооценкой (это Волна 2 после measurements/ROM)
- НЕ создавать cron в scheduler.js (отдельный коммит для инструкторской стороны после Волны 0)
- НЕ создавать таблицу `phase_criteria` (Волна 2)
- НЕ трогать `Patients.js` (инструкторская сторона)

В этом коммите — только баннер для пациента + endpoint + pre-filled message механика.

## Критичные правила

⛔ Те же. STOP после commit, smoke в браузере (включая искусственное застревание через UPDATE phase_started_at), один коммит за сессию.

## После закрытия #06 — финал волны

После того как этот коммит в main, **ОБЯЗАТЕЛЬНО:**

1. Обнови `wave_0_progress.md` — все 6 строк должны быть 🟢
2. Обнови `CLAUDE.md` секцию «Завершённые исправления» консолидированной записью «Wave 0 — 6 коммитов закрыты, регресс v12 streak починен, литерал ПКС убран, отчёт в messages, повторный заход разблокирован, accordion упражнений расширен, stuck banner добавлен»
3. Обнови `CLAUDE.md` секцию «Открытые баги» — вычеркни все пункты которые закрыты Волной 0
4. Создай файл `memory/wave_0_closed.md` с датой закрытия и SHA первого/последнего коммитов
5. Скажи мне в чате: «Волна 0 закрыта, готов финальный smoke на 2-3 пациентах разного состояния»

После моего финального smoke в проде на разных пациентах — Волна 0 = done, ждём от архитектора `TZ_WAVE_1_INDEX.md`.

## Поехали
```

---

## Универсальный шаблон для будущих волн

Когда пойдёт Волна 1 / 2 / 3 — шаблон будет таким же. Меняется только:

```
[НОМЕР ВОЛНЫ] — например, 1
[НОМЕР КОММИТА] — например, 03
[ИМЯ ФАЙЛА ТЗ] — например, TZ_WAVE_1_03_program_templates_seed.md
[НОМЕР В ROADMAP] — например, #3
[ТЕМА ROADMAP] — например, шаблоны программ
[ЧТО НЕ ДЕЛАТЬ] — список не-в-этом-коммите вещей
```

Архитектор в каждой волне даёт обновлённый INDEX и пакет промптов.

---

## Что Vadim делает между сессиями

После каждого «🟡 готов к smoke» от Claude Code:

1. Открой DevTools в браузере
2. Войди как пациент `avi707@mail.ru` / `Test1234` (или другая роль если нужно)
3. Пройди ВСЕ smoke-сценарии из ТЗ коммита, не только сценарий 1
4. Проверь light + dark тему (ThemeToggle на ProfileScreen)
5. Проверь mobile layout (DevTools → Toggle device → iPhone 14 Pro Max)
6. Если что-то не работает — пиши в Claude Code «откатить» или «исправить вот это»
7. Если всё ОК — пиши «ок, push в main». Только тогда Claude Code пушит.
8. После push — проверь GitHub Actions deploy лог (CI должен зелёный)
9. После деплоя — проверь my.azarean.ru как пациент `vadim@azarean.com` / `Test1234`
10. Если прод сломан — ROLLBACK немедленно (`git revert HEAD && git push`), потом разбираемся
11. Если прод ок — обнови `wave_0_progress.md` строку «Push в main: ✅ дата»

---

## Если Claude Code сделал не то

Стандартные команды:

- «Откати последний commit» → `git reset --hard HEAD~1`
- «Покажи diff что ты собираешься коммитить» → `git diff --staged`
- «Не пушь, я ещё проверю» → ждёт
- «Создай отдельный feature-branch для эксперимента» → `git checkout -b experiment/...`
- «Удали свою последнюю миграцию» → файл удалить + `wave_0_progress.md` пометить как откат

Если Claude Code продолжает делать не то 2-3 раза подряд → закончить сессию, начать новую с улучшенным промптом.
