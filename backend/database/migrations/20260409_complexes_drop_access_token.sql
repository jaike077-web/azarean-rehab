-- =====================================================
-- Миграция: удаление поля complexes.access_token
-- 2026-04-09
-- =====================================================
--
-- Финальный этап миграции от публичных ссылок к личному кабинету.
-- Весь код уже не ссылается на access_token (см. этапы 1-5).
-- Удаляем колонку и её UNIQUE индекс.
-- =====================================================

DROP INDEX IF EXISTS idx_complexes_token;
ALTER TABLE complexes DROP COLUMN IF EXISTS access_token;
