-- 2026-06-25: program_types — 2 новых типа: эндопротезирование плечевого сустава.
-- Shoulder protocols, новая партия (артропластика). Под-волна A1.
--
-- Источник: 2 доказательных досье (PROTOCOLS/shoulder/shoulder_{tsa,rsa}*.md)
-- + PROTOCOLS/shoulder/_extractions_newbatch.json (объекты shoulder_tsa / shoulder_rsa).
-- Это ТОЛЬКО справочник типов. Контент фаз + критерии — отдельными миграциями (_02, _03).
--
-- Решения (Vadim 2026-06-22): артропластика идёт первой под-волной новой партии плеча.
-- TSA (анатомическое) и RSA (реверсивное) — два РАЗДЕЛЬНЫХ типа: защитные оси зеркальны
-- (TSA бережёт шов подлопаточной → запрет ранней наружной ротации и активной внутренней;
-- RSA бережёт дельтовидную + анти-дислокационные предосторожности → запрет «рука за спину»
-- и комбинации разгибание+приведение+внутр.ротация). Внутрихирургические варианты (тип доступа,
-- перенос сухожилий, перелом с фиксацией бугорков) — модификаторы в тексте фаз, НЕ отдельные типы.
--
-- surgery_required=TRUE для обоих — инхерентно послеоперационные (эндопротез).
-- joint='shoulder'; body_side_relevant=TRUE (дефолт).
--
-- Идемпотентна: ON CONFLICT (code) DO NOTHING — повторный прогон ничего не меняет,
-- ручные правки label/position через AdminContent сохраняются.

BEGIN;

INSERT INTO program_types (code, label, joint, surgery_required, position) VALUES
  ('shoulder_tsa', 'Анатомическое эндопротезирование плеча',  'shoulder', TRUE, 30),
  ('shoulder_rsa', 'Реверсивное эндопротезирование плеча',    'shoulder', TRUE, 31)
ON CONFLICT (code) DO NOTHING;

COMMIT;
