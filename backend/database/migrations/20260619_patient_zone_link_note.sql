-- 20260619_patient_zone_link_note.sql
-- Мультипрограммный «Путь» (M2.1 — связь зон).
-- Авторская инструктор-нота «как одна зона влияет на другую» (напр. слабость
-- ягодичных при дисфункции ТБС → перегрузка колена). Хранится НА ПАЦИЕНТЕ
-- (не на программе): нота про весь «Путь», переживает смену приоритета зон.
-- Показывается пациенту карточкой вверху «Пути» только при ≥2 активных зонах.
--
-- Аддитивно и идемпотентно. Без default/CHECK/индекса — обычный свободный текст
-- (длина ограничивается на application-layer, как notes/diagnosis).

ALTER TABLE patients
  ADD COLUMN IF NOT EXISTS zone_link_note TEXT;
